import React, { useState } from 'react'

const ChipInputBox = () => {
      const [chips, setChips] = useState(["nana", "nino", "nii-chan"]);

  return (
    <div>ChipInputBox</div>
  )
}

export default ChipInputBox